# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules

# Scratch & development scripts
scratch/
scripts/rfwiki-raw/
scripts/scrape-rfwiki.js
scripts/generate-ori-md.js
check_image.js
rfdatabase-ori.md
nginx_focus_rpg.conf

dist
dist-ssr
*.local
.env
.env.local

# Local-only experiment page — dressing/gear calibration sandbox, never deploy
public/dressing-lab.html
public/dressing-room.html

# Server data (user saves, sessions) — jangan di-commit
# NOTE: /data/ (root only) not src/data/ (game data must be committed)
/data/
vps_key
deploy.ps1

# Runtime state written by server.js — never commit, causes pull conflicts
# between the VPS's live-updated copy and whatever's in the repo
chip_war_data.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.token-savior-cache.json
